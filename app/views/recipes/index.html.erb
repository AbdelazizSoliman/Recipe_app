<%= render partial: 'shared/navbar' %>

<div class="flex flex-col gap-5 w-full md:w-[60%] mx-auto">
    <% if @recipes.present? %>
    <% @recipes.each do |recipe| %>
    <%= render partial: 'shared/recipe_card', locals: { recipe: recipe } %>
    <% end %>
    <% else %>
    <p>No recipes found.</p>
    <% end %>
</div>